{
  "name": "messaging-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Static-IP HTTP proxy for SMS and SMTP providers (deployed on VPS via Coolify).",
  "type": "module",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.7",
    "hono": "^4.6.14",
    "nodemailer": "^6.9.16"
  },
  "engines": {
    "node": ">=20"
  }
}
